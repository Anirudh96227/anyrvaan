{
	"manifest_version": 3,
	"name": "Negative Space",
	"version": "1.0.0",
	"description": "Strip any page to its skeleton — no color, no imagery, no decoration. Only structure, type, and whitespace remain.",
	"icons": {
		"128": "icon.png"
	},
	"action": {
		"default_icon": "icon.png",
		"default_title": "Negative Space — strip this page"
	},
	"permissions": ["activeTab", "scripting"],
	"background": {
		"service_worker": "background.js"
	}
}
